using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSight : MonoBehaviour {

    public float enemyinView; //Angle that tells us how close the player's viewing angle is to enemy;
    public Vector3 enemyDirection; //Direction of the enemy;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
